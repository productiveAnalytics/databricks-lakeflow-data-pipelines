[project]
name = "databricks-lakeflow-scd-demo"
version = "0.1.0"
description = "Lakeflow AUTO CDC SCD Type 1 and Type 2 reference implementation with DQX and Declarative Automation Bundles"
requires-python = ">=3.10"
dependencies = [
  "pytest>=8.0,<9",
  "pyyaml>=6.0,<7",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
